<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ha_pla">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.HaPla">

        <activity android:name=".Login" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity android:name=".Start" android:exported="true">
        </activity>

        <activity android:name=".Haushalt" android:exported="true">
        </activity>

        <activity android:name=".Einkaufen" android:exported="true">
        </activity>

        <activity android:name=".Putzen" android:exported="true">
        </activity>

        <activity android:name=".ToDo" android:exported="true">
        </activity>

        <activity android:name=".variableKosten" android:exported="true">
        </activity>

        <activity android:name=".FixKosten" android:exported="true">
        </activity>

        <activity android:name=".Registrierung" android:exported="true">
        </activity>
    </application>

</manifest>